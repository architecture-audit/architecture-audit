import { defineBackend } from '@aws-amplify/backend';

/**
 * AI Architecture Audit - Static Website
 * Deployment region: ap-south-1 (Mumbai)
 */
defineBackend({
  // No auth or data needed for static site
});
